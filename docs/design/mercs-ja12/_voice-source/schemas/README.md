# Speech stem schemas (схема реплик)

Source: `Downloads/ja2mercs (1)/ja2mercs/схема реплик *.xlsx`.

| File | Scope |
| --- | --- |
| `схема реплик AIM.xlsx` | AIM hire/combat stems (`XXX_000`…`XXX_116` + battle) |
| `схема реплик MERC.xlsx` | MERK (no full hire 081–120 expected) |
| `схема реплик IMP.xlsx` | IMP banks |
| `схема реплик RPC *.xlsx` | Per-RPC overlays |
| `AIM-stem-roles.md` | Markdown export of AIM schema (Bayun/ship tables) |

Stem role → Jazz: `JA2_SPEECH_ID_RANGES.md` + `AIM_CHAT_WAV` / `SLOT_WAV` in `_ship_ja2_merc_voices.py`.
