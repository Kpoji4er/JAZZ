---
description: >-
  Read when editing an active/accepted spec or when runtime numbers, IDs,
  or acceptance drifted from the spec.
globs:
  - docs/specs/**
alwaysApply: false
---

# Spec sync: спрашивать перед изменением контракта

`docs/specs/` — утверждённый контракт. Код и current-state docs не должны молча расходиться со spec.

## Когда спросить владельца (до правки)

1. Меняешь поведение, defaults, public ID, schema, caps/costs или acceptance, уже зафиксированные в `docs/specs/active|accepted/*.md`.
2. Аудит находит, что `implemented`/`approved` spec **отстаёт от кода**.
3. Хочешь пометить AC как superseded, менять Evidence PASS→FAIL, или переносить scope в другой SPEC-ID.
4. Код уже загружен, а spec всё ещё `approved` с BLOCKED runtime AC — спросить: поднять status / сузить docs / выгрузить код.

## Как спрашивать

SPEC-ID + поле/AC, что предлагает код, варианты: обновить spec / откатить код / пометить superseded / partial.

Не обновлять locked contract «заодно» без ответа, кроме явного «правь спеки» / «синхронизируй».

После согласия: spec в том же change set (или отдельный docs-only). Current-state — в `docs/technical/`. Evidence: `PASS`/`FAIL`/`BLOCKED` + уровень (static/editor/runtime/human).

Связано: `docs/specs/README.md`, `$specify-jazz-change`.
